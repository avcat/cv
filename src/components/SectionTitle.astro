---
import { slugifyTitle } from '@helpers/helpers.js';

interface Props {
  title: string;
}

const { title } = Astro.props;
---
{title && (
  <h3 id={slugifyTitle(title)} class="mt-4">
    {title}
  </h3>
)}